import type {Config} from 'tailwindcss';
const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        inter: ['Montserrat', 'sans-serif'],
      }
    },
  },
};
export default config;
